# 支付宝小程序示例

1. 在 `examples/alipay/app.js` 里把 `dsn` 替换成你自己的
2. 开发时生成 `vender/sentry-miniapp.js`：`pnpm dev:alipay`
3. 用支付宝小程序开发者工具打开 `examples/alipay`

> 如果要测试 `my.request`，需要在支付宝小程序后台配置 request 合法域名。

